
<div class="category-menu-title">
	<svg xmlns="http://www.w3.org/2000/svg" style="color: fff;" width="24" height="24" fill="currentColor" class="bi bi-list" viewBox="0 0 16 16">
  <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5z"/>
</svg>
	{{text_category}}</div>
<ul class="category-menu-nav">
	{% for category in categories %}
		{% if category.children %}
			<div class="category-item" style="position: relative">
				<div class="category-item__inner">
					<a class="category-link active" href="{{ category.href }}">{{ category.name }}</a>
					<div class="category-popup-sub-menu category-popup-sub-menu_main">
						<div class="category-popup-sub-menu__inner">
							<div class="category-popup-sub-menu__item">
								<div class="category-popup-sub-menu__link-wrapper">
									{% for child in category.children %}
										<a class="subcategory-link" href="{{ child.href }}">{{ child.name }}</a><br>
									{% endfor %}
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		{% else %}
			<a class="category-link active" href="{{ category.href }}">{{ category.name }}</a>
		{% endif %}
	{% endfor %}
</ul>
<style>
@media (min-width: 769px) {
  .category-menu-title {
    display: block;
  }
  .category-menu-nav {
    display: block;
  }
  .category-item {
    position: static;
    width: 100%;
    margin-bottom: 10px; /* примерная высота для удобства просмотра */
  }
  .category-item__inner {
    display: block;
    width: 100%;
  }
  .category-popup-sub-menu {
    position: static;
    display: none;
  }
  .category-popup-sub-menu__inner {
    display: block;
    width: 100%;
  }
  .category-popup-sub-menu__item {
    display: block;
    width: 100%;
  }
  .category-popup-sub-menu__link-wrapper {
    display: block;
    width: 100%;
  }
  .subcategory-link {
    display: inline-block;
    width: auto;
    white-space: nowrap;
  }
}
</style>